import 'package:flutter/material.dart';
import 'package:flutter_mobile/udemy_max/quiz_app/quiz.dart';

class MainQuizApp extends StatelessWidget {
  const MainQuizApp({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Quiz();
  }
}
